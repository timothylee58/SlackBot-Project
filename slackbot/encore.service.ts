import { Service } from "encore.dev/service";

// Encore service declaration for the SlackBot app.
// The Express app (backend/) is mounted as a raw HTTP handler below.
export default new Service("slackbot");
